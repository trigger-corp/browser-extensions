These files are used to generate files used in v2.

The 'forge' folder will be copied into the app as is, concatenated/minified versions of the JS files will then be added to that folder for each platform.